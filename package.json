{
  "name": "ccfrescue-stripe-backend",
  "version": "1.0.0",
  "description": "Production backend for Chester & Chubbs Foundation Stripe donations",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "set NODE_ENV=development && node index.js",
    "prod": "set NODE_ENV=production && node index.js",
    "setup-dev": "copy env-development.txt .env.dev && echo Development environment configured. Please edit .env.dev with your database credentials.",
    "setup-prod": "copy env-production.txt .env && echo Production environment configured. Please edit .env with your Hostinger database credentials."
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "nodemailer": "^6.9.7",
    "multer": "^2.0.0-rc.4",
    "mysql2": "^3.6.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

